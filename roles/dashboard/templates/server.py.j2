import os
from http.server import HTTPServer, CGIHTTPRequestHandler

os.chdir('/opt/homer/web')

server_object = HTTPServer(server_address=('', {{ dashboard_port }}), RequestHandlerClass=CGIHTTPRequestHandler)

server_object.serve_forever()