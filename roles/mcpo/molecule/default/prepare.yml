---
- name: Prepare
  hosts: all
  tasks:
    - name: Ensure python3 and pip are installed.
      ansible.builtin.package:
        name:
          - python3
          - python3-pip
        state: present
        update_cache: true
