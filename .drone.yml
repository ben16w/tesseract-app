---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: lint
    commands:
      - yamllint .
      - ansible-lint
  - name: test
    commands:
      - make test